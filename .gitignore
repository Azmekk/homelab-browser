# Build artifacts (all binaries live in /bin at the repo root)
/bin/

# Local data (SQLite DB + uploaded icons)
/data/
/src/data/

# Dev env files
.env
/src/.env

# Node (only used if you ever run a helper script locally)
node_modules/
package-lock.json
